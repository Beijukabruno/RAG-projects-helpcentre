# Paste this whole file into the GitHub secret named ENV_FILE_CONTENTS.
# Keep real values only in GitHub secrets or on the VM, not in git.

APP_NAME=helpcentre

# The workflow appends HELPCENTRE_IMAGE automatically for each commit.
# HELPCENTRE_IMAGE=your-dockerhub-user/helpcentre-api:latest

# Host-to-container port mapping in docker-compose.yml:
# HELPCENTRE_API_HOST_PORT:8000
# For this deployment, the API will be reachable on host port 8080.
HELPCENTRE_API_HOST_PORT=8080

POSTGRES_HOST_PORT=5432
POSTGRES_USER=helpcentre_user
POSTGRES_PASSWORD=replace-with-a-strong-password
POSTGRES_DB=helpcentre_db

GEMMA_API_KEY=replace-with-google-genai-key
GOOGLE_API_KEY=replace-with-google-genai-key
GEMMA_MODEL=models/gemma-4-31b-it
GEMMA_FALLBACK=models/gemma-2.5-flash

EMBEDDING_PROVIDER=gemini
EMBEDDING_MODEL=models/gemini-embedding-2
EMBEDDING_DIM=1536
VECTOR_BACKEND=postgres

CORS_ALLOW_ORIGINS=*
JWT_SECRET=replace-with-a-long-random-secret

BOOTSTRAP_SUPER_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_SUPER_ADMIN_PASSWORD=replace-with-a-strong-password

INDEX_PROJECTS=tb,cervical_cancer
CHUNKING_STRATEGY=semantic
BOOTSTRAP_INDEX_ON_START=0
WAIT_FOR_DB=1
DB_WAIT_TIMEOUT_SECONDS=90
BATCH_SIZE=64

RERANKER_ENABLED=true
RERANKER_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
RERETRIEVAL_K=20
