# Reports

This folder is used by `scripts/inspect_admin_endpoints.py` to save admin endpoint inspection outputs.

Each run creates a timestamped subdirectory under `reports/admin-endpoint-inspection/` with raw endpoint responses and a summary file.